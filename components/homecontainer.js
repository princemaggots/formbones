import Head from 'next/head'
import Link from 'next/link'
import styles from './homecontainer.module.css'


export default function HomeContainer() {
    return (
        <div className={styles.containerhome}>
            <div className={styles.welcome}>
                <h1> Welcome!</h1>
                <p>Website routing protocol fsociety worm network wipe. Password off the grid decrypt worm breach Wi-Fi computer nodes. Log file connect fiber connection dat file RUDY attack cyber security Tor wipe all the data backup. Protocol fsociety computer, compromised worm terminal routing protocol password 100 terabytes Tor hack boot up. Wi-Fi malware DDoS attack, data center exit nodes 100 terabytes computer DNS encryption sys admin rootkit intercepting traffic virus. Data center network traffic delete reboot Wi-Fi operating system fsociety wipe backup two-step verification code disconnect. Backup rootkit malware breach compromised Wi-Fi network IRC hack reboot worm.</p>

                <p>Password protocol terminal code traffic. DNS IRC password system routing protocol exit nodes worm. Boot up server farm IRC terminal, log file AFK data center Wi-Fi traffic bonsoir. System files data dump decrypt terminal, fiber connection computer bonsoir worm server farm anonymous Tor timing out sys admin.</p>
            </div>
            <div className={styles.homeimg}><img src="https://via.placeholder.com/500"/></div>
        </div>
    )
}